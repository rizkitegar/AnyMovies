package com.movies.anymovies.core.uilegacy.state

/** Endless-scroll footer state, shared by any `RecyclerView` that paginates. */
public enum class AppendFooterState { IDLE, LOADING, ERROR, END_REACHED }
